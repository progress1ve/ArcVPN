# Debug Session: subscription-503-import-500

- Status: OPEN
- Symptoms:
  - `/sub/<active_sub_id>?format=plain` returns `503 Subscription temporarily unavailable`
  - `/import/<active_sub_id>` returns `500 Internal Server Error`
- Scope:
  - Runtime debugging on production server via user-provided command output
- Hypotheses:
  - H1: `_response_from_prepared()` now sets a header value rejected by Werkzeug/WSGI.
  - H2: `/import/<sub_id>` fails while building HTML or response headers.
  - H3: `_generate_links_for_keys()` or XUI fetch now times out / raises and bubbles into `503`.
  - H4: Existing production config/data shape triggers an exception not seen locally.
- Evidence Needed:
  - Full traceback from `journalctl` around both failing requests
  - Direct grep for traceback/error lines in service logs
  - Optional isolated python invocation if traceback is ambiguous
- Evidence Collected:
  - Before restart, old process served `/sub/...` successfully for active keys at `17:53`.
  - New process started at `18:04:36` with PID `843772`.
  - User-observed failures (`503` and `500`) happened at `18:08`, but corresponding traceback lines are not yet captured in the provided journal output.
- Hypothesis Status:
  - H1: rejected; no header-related traceback found.
  - H2: confirmed. Root cause is local variable shadowing: `html = f"""..."""` inside `import_to_happ()` makes `html.escape(...)` fail with `UnboundLocalError`.
  - H3: confirmed. Production `bot/services/panels/xui.py` does not have `get_client_configs()`, so new `subscription_api.py` is incompatible with the deployed XUI client and returns `503`.
  - H4: rejected; issue is code version mismatch, not data shape.
